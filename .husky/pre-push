echo "Running pre-push checks..."

# Build check
echo "→ Building project..."
pnpm build || exit 1

# Unit tests
echo "→ Running unit tests..."
pnpm test || exit 1

# E2E tests (enable when needed)
# echo "→ Running E2E tests..."
# pnpm test:e2e || exit 1

echo "All checks passed. Ready to push."